image: python:latest

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

cache:
  paths:
    - .cache/pip
    - venv/  # Aggiungi l'ambiente virtuale alla cache

stages:
  - build
  - verify
  - unit-test
  - integration-test
  - package
  - release

before_script:
  - python --version ; pip --version  # For debugging
  - pip install virtualenv
  - source venv/bin/activate

build:
  stage: build
  script:
    - pip install -r requirements.txt
verify:
  stage: verify
  script:
    - prospector  # Esegui l'analisi statica con Prospector
unit-test:
  stage: unit-test
  script:
    - pytest tests/test_unit.py
integration-test:
  stage: integration-test
  script:
    - pytest tests/test_integration.py
package:
  stage: package
  script:
    - python setup.py sdist bdist_wheel
    - ls dist/
  artifacts:
    paths:
      - dist/
release:
  stage: release
  script:
    - source venv/bin/activate
    - echo "[pypi]" > ~/.pypirc
    - echo "username = __token__" >> ~/.pypirc
    - echo "password = pypi-AgEIcHlwaS5vcmcCJDMxZWQyZTEzLTRiMzItNGJmZS05OGZiLTU5MmI2OTk2NTQ5MgACKlszLCI0M2IxOGI3Yi03MjA4LTQ4YmMtOWUwMC0yZmYzMDJhYjc4NDUiXQAABiBAurrrvGNd-JpEl4yUhspegrrmBbeu0oX-fbh3sQgS" >> ~/.pypirc
    - cat ~/.pypirc  # Stampa il contenuto del file .pypirc per scopi di debug
    - twine upload dist/*




